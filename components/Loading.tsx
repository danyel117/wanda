import ReactLoading from 'react-loading';

const Loading = () => <div>Loading...</div>;

const MiniLoading = () => <ReactLoading type='spin' height={20} width={20} />;

export { MiniLoading };

export default Loading;
